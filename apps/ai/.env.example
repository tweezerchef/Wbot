# ============================================================================
# Wbot AI Backend - Environment Variables
# ============================================================================
# Copy this file to .env and fill in your values.
# Never commit .env to version control!
# ============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Get these from your Supabase project: Settings > API

# Your Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# The service role key (NOT the anon key)
# This is used for server-side auth validation
SUPABASE_SERVICE_KEY=your-service-role-key

# Database password for direct PostgreSQL connection
# Get from: Supabase Dashboard > Settings > Database > Connection string
# Look for the password in the connection string (after postgres: and before @)
SUPABASE_DB_PASSWORD=your-database-password

# -----------------------------------------------------------------------------
# PostgreSQL Checkpointing (Self-Hosted LangGraph)
# -----------------------------------------------------------------------------
# Required for persistent conversation state in self-hosted deployments.
# Use Supavisor Session Mode pooler (IPv4 compatible - works in Docker)
# Format: postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
# Get from: Supabase Dashboard > Settings > Database > Connection string (Session mode)
DATABASE_URI=

# -----------------------------------------------------------------------------
# LLM Configuration
# -----------------------------------------------------------------------------
# Set the provider and API keys for the language models

# Which LLM provider to use: "anthropic" or "google"
LLM_PROVIDER=anthropic

# Anthropic API Key (for Claude)
# Get it from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key

# Google API Key (for Gemini)
# Get it from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your-google-api-key

# OpenAI API Key (for meditation TTS)
# Get it from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
# Used for LangGraph pub/sub (streaming) and embedding cache.
# In production (Docker), this is provided by the langgraph-redis service.
# For local dev, you can run: pnpm redis:local:start
#
# REDIS_URI is the LangGraph convention (preferred)
# REDIS_URL is also supported for backwards compatibility
REDIS_URI=redis://localhost:6379

# -----------------------------------------------------------------------------
# LangSmith Observability (Optional)
# -----------------------------------------------------------------------------
# Enable tracing and observability for your LangGraph deployment.

# Enable/disable tracing (default: false)
LANGSMITH_TRACING=false

# Your LangSmith API key
# Get from: https://smith.langchain.com/settings
LANGSMITH_API_KEY=

# Project name for organizing traces
LANGSMITH_PROJECT=wbot-wellness
