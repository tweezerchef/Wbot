/* ============================================================================
   WellnessProfile Export
   ============================================================================ */

export { WellnessProfile } from './WellnessProfile';

// Re-export types from consolidated types file
export type { WellnessProfileProps, WellnessStats } from '../../types';

export {
  getBaselineLabel,
  getBaselineColor,
  EMOTIONAL_BASELINE_LABELS,
  EMOTIONAL_BASELINE_COLORS,
} from '../../types';
