# ============================================================================
# Web App Environment Variables
# ============================================================================
# Copy this file to .env and fill in your values
# IMPORTANT: VITE_ prefix is required for variables exposed to the client
# ============================================================================

# Supabase Configuration
# Get from: https://app.supabase.com/project/_/settings/api
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# LangGraph API (AI Backend)
# Local development: http://localhost:2024 (LangGraph CLI default port)
# Production: Your self-hosted LangGraph deployment URL
VITE_LANGGRAPH_API_URL=http://localhost:2024

# -----------------------------------------------------------------------------
# Redis Cache (Server-Side Only)
# -----------------------------------------------------------------------------
# Used for caching conversation messages. Same Redis instance as AI backend.
# If not set, caching is disabled and all reads go directly to Supabase.
#
# Use Upstash or similar remote Redis so both web and AI backend share the cache.
# Copy the REDIS_URL from the root .env or apps/ai/.env
REDIS_URL=
